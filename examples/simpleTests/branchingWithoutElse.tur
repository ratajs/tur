output = 0;
if ρ input > 0:
	output = 10;
	if input[0] > 5:
		output = 20;
		if (ρ input)==2:
			output = 3;
		endif;
		if ρ input > 1 and input[1]==2:
			output = 4;
			if input[0]==11 or ρ input > 3:
				output = 6;
			endif;
		endif;
	endif;
endif;
