output = [];

x = 10;
loop:
	output[] = x;
	if ρ output > x:
		break;
	endif;
	output[] = (ρ output);
	x−−;
endloop;

loop:
	loop:
		output[] = 42;
		break;
	endloop;
	output[] = ρ input;
	if (ρ input)==0:
		break;
	else:
		input = input[1:];
	endif;
endloop;
