# Requires: function g
# Input: any preceding input, number x
# Output: sum of g(input..., x)[0] for all y < x

require g;

[input..., y] = input;

output = 0;
while y > 0:
	y−−;
	output = output + g(input..., y);
endwhile;
