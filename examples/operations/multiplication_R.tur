# Requires: function g
# Input: any preceding input, number x
# Output: product of g(input..., y)[0] for all y < x

require g;

[input..., y] = input;

output = 1;
while y > 0:
	y−−;
	output = output ⋅ g(input..., y);
endwhile;
