# Requires: function g
# Input: number x, any following input
# Output: product of g(y, input...)[0] for all y < x

require g;

[y, input...] = input;

output = 1;
while y > 0:
	y−−;
	output = output ⋅ g(y, input...);
endwhile;
