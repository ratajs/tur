# Requires: function g, function h
# Input: any preceding input, number y
# f(input.., 0) = g(input...)
# f(input..., y) = h(input..., y − 1, f(input..., y − 1)) for y > 0
# Output: f(input..., y)

require g;
require h;

[input..., limit] = input;

output = g(input...);
y = 0;
while y < limit:
	output = h(input..., y, output);
	y++;
endwhile;
