# Requires: function g
# Input: any input
# Output: miminum y such that g(y, input...)[0] is 0 and for all z < y the machine returns a valid number; does not halt if there is no such y

require g;

output = 0;
while g(output, input...)!=0:
	output++;
endwhile;
