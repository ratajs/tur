# Requires: function g
# Input: number x, any following input
# Output: sum of g(y, input...)[0] for all y < x

require g;

[y, input...] = input;

output = 0;
while y > 0:
	y−−;
	output = output + g(y, input...);
endwhile;
